name: Cross-Platform CI & Build

on:
  push:
    branches: [ main, master ]
  pull_request: 
    branches: [ main, master ]

jobs:
  build-and-test:
    name: Build & Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux GUI Dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev libgtk-3-dev libxcb-render0-dev libxcb-shape0-dev libxcb-xfixes0-dev libxkbcommon-dev libasound2-dev

      - name: Run Cargo Test
        run: cargo test -- --nocapture

      - name: Build Release Binary
        run: cargo build --release

      - name: Prepare Windows Distribution
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist
          Copy-Item target/release/dupesweeper.exe dist/DupeSweeper-Setup.exe
          Copy-Item target/release/dupesweeper.exe dist/DupeSweeper.exe

      - name: Upload Windows Setup Installer
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: DupeSweeper-Setup
          path: dist/DupeSweeper-Setup.exe

      - name: Upload Windows Portable
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: DupeSweeper-Portable
          path: dist/DupeSweeper.exe

      - name: Upload Linux Binary
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: dupesweeper-linux-x86_64
          path: target/release/dupesweeper

      - name: Upload macOS Binary
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: dupesweeper-macos-universal
          path: target/release/dupesweeper

